// Supported languages in the application (words, lectures, explainsLanguage, etc.)
export type Language = "en" | "es" | "pt" | "it" | "fr";

/** Idioma que estudias (User.language): sin español */
export type ContentLanguage = "en" | "pt" | "it" | "fr";
